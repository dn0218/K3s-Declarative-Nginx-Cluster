apiVersion: v1
kind: Service
metadata:
  name: nginx-ha-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx-ha
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
